# ChartBridge AI — environment. EVERYTHING is optional: with zero keys the app
# runs fully on the live FHIR sandbox with a deterministic report + lexical RAG.
# Add keys to light up Grok generation and a real vector DB.

# --- xAI Grok (report generation; voice handled separately) ---
# Without this, reports use the deterministic reconciliation engine.
XAI_API_KEY=
# Optional override; defaults to grok-4.3 (June 2026 flagship).
# GROK_MODEL=grok-4.3

# --- xAI Collections (recommended: hosted vector DB for the CRM RAG corpus) ---
# Embeddings are done server-side by xAI (grok-embedding-small) — no OpenAI needed.
# 1. Management key (xAI console -> Management Keys, grant Collections access):
XAI_MANAGEMENT_API_KEY=
# 2. Seed the corpus once: `curl -X POST localhost:3000/api/collections/seed`
#    then paste the returned collectionId here and restart:
XAI_COLLECTION_ID=

# --- OpenAI (only for the legacy in-memory / turbopuffer RAG path) ---
# Not needed when XAI_COLLECTION_ID is set. Leave blank to skip.
OPENAI_API_KEY=

# --- turbopuffer (legacy alternative vector DB; needs OPENAI_API_KEY) ---
# Superseded by xAI Collections above. Without these, RAG falls back to lexical.
TURBOPUFFER_API_KEY=
TURBOPUFFER_REGION=gcp-us-central1

# --- FHIR (defaults to the zero-auth SMART Health IT launcher) ---
# FHIR_BASE_URL=https://launch.smarthealthit.org/v/r4/fhir
# DEFAULT_PATIENT_ID=5fbee500-9b99-4dad-bce4-0a4de9f9f3b9   # "Marsha Hayes"

# --- Inngest (autonomous pipeline; optional) ---
# Local: run `npx inngest-cli@latest dev` and set INNGEST_DEV=1.
# Vercel: install the Inngest integration (auto-sets the keys below).
# INNGEST_DEV=1
# INNGEST_SIGNING_KEY=
# INNGEST_EVENT_KEY=
